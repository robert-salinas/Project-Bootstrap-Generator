# 1. Initial Architecture

Date: 2023-11-20

## Status

Accepted

## Context

We need to establish the initial architectural structure for {{ project_name }}. 
The project type selected is `{{ project_type }}` with the following modules enabled:
- Git: {{ with_git }}
- Docker: {{ with_docker }}
- GitHub Actions: {{ with_github_actions }}
- Virtual Env: {{ with_venv }}

## Decision

We will use the scaffolding generated by RS Project Bootstrap Generator as the base directory layout.
Each module will follow its respective standard structure.

## Consequences

Development will start with a standardized layout, allowing quicker onboarding and minimal boilerplate setup.
